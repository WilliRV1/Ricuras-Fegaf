export default function CocinaPage() {
  return (
    <main style={{ padding: '2rem', color: 'var(--color-text)' }}>
      <h1>Cocina (KDS)</h1>
      <p>Tablero de comandas en tiempo real — próximamente.</p>
    </main>
  );
}
